---
description: Sync meaningful project context changes back into cotext.
---

Summarize the new design, note, progress, todo, or deferred changes that came out of the current work on `cotext`. Then run the appropriate `cotext new` or `cotext update` commands so the project context stays current.
